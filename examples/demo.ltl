// examples/demo.ltl — runnable demo for ltl_skiplist

import std::io
use ltl_skiplist as lib

fn main() {
    io::println("ltl_skiplist demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}